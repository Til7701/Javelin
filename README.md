# Javelin

Javelin is an absolutely serious low performance programming language.
It does not have any loops or if-statements. Instead `when`-statements are used to react to changes.

## Language Overview

TODO

## Build

To build Javelin you need Java 27 or newer installed.
Then you can run:

```shell
./mvnw clean package
```

Then you can run the CLI using:

```shell
java -jar cli/target/cli-1.0-SNAPSHOT-jar-with-dependencies.jar
```

A more convenient packaging has not been set up yet, but is planned.
